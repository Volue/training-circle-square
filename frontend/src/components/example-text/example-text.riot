<example-text>
    <p>{ state.value }</p>
    <script>
    export default {
        onMounted() {
            this.appState.exampleText = this
            this.update({
                value: 'Click on new game button to start game!'
            })
        }
    }
  </script>
</example-text>